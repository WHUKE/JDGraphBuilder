[project]
name = "jdgraphbuilder"
version = "0.1.0"
description = "核心系统 — 数据清洗、图建模、入库"
requires-python = ">=3.12"
dependencies = [
    "neo4j>=6.1.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
